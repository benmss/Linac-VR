using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIAnchor : MonoBehaviour {
  public Transform anchor;
  
  // public GameObject handAnchor1;
  // public GameObject handAnchor2;
  
  // Vector3 offset = new Vector3(-0.017f,0.111f,0.187f);
  // Vector3 rot = new Vector3(22.97f,0f,0f);
  
  // bool attached = false;  

  // void AttachToHand(int h) {
    // if (h == 1) {
      // transform.parent = handAnchor1.transform;
    // } else if (h == 2) {
      // transform.parent = handAnchor2.transform;
    // }
  // }
  
	// Use this for initialization
	// void Start () {
		// if (!attached && handAnchor1.activeInHierarchy) { AttachToHand(1); }
	// }
	
	// Update is called once per frame
	// void Update () {
		
	// }
}
